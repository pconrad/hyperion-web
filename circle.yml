machine:
  node:
    version: 6.1.0
test:
  override:
    - npm test
    - npm run build-prod
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/zip/
    - pushd build && zip -9vr $CIRCLE_TEST_REPORTS/zip/hyperion-web.zip . && popd
